<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div>
        <% if(shops!=null){ %>
        <% shops.forEach(shop => { %>
            <div><a href="user/shop/<%=shop.sellerId%>"><%=shop.name%></a></div>
          <% }) %>
          <%} else{%>
                 <div>No Shop is currently available</div>
            <% }%>
    </div>
</body>
</html>