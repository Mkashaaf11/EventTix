class User {
  constructor(username, email, password, age, cityId) {
    this.username = username;
    this.email = email;
    this.password = password;
    this.age = age;
    this.cityId = cityId;
  }

  // You can add methods to the class as needed
}

module.exports = User;
